pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/AlexPCoder/Final.git', branch: 'main'
            }
        }
    }
}
